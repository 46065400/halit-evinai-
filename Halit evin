<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Halit Evin Aİ | Akıllı Ev Asistanı</title>
    <!-- Tailwind CSS - Modern tasarım için -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome İkonları -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: #f8fafc;
            color: #1e293b;
            margin: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .ai-gradient-bg {
            background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
        }
        .glass-effect {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(226, 232, 240, 0.8);
        }
        .message-bubble {
            max-width: 85%;
            padding: 14px 18px;
            border-radius: 20px;
            margin-bottom: 12px;
            font-size: 0.95rem;
            line-height: 1.6;
            animation: slideIn 0.3s ease-out forwards;
            word-wrap: break-word;
        }
        .ai-message {
            background-color: white;
            color: #1e293b;
            border-bottom-left-radius: 4px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            align-self: flex-start;
        }
        .user-message {
            background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
            color: white;
            border-bottom-right-radius: 4px;
            align-self: flex-end;
            box-shadow: 0 4px 6px -1px rgba(37, 99, 235, 0.2);
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        /* Yükleniyor animasyonu */
        .dot-pulse { display: flex; gap: 4px; padding: 10px; }
        .dot { width: 8px; height: 8px; background: #3b82f6; border-radius: 50%; animation: pulse 1.4s infinite ease-in-out; }
        .dot:nth-child(2) { animation-delay: 0.2s; }
        .dot:nth-child(3) { animation-delay: 0.4s; }
        @keyframes pulse { 0%, 80%, 100% { transform: scale(0.3); opacity: 0.3; } 40% { transform: scale(1); opacity: 1; } }
        
        .chat-area {
            height: calc(100vh - 160px);
        }
    </style>
</head>
<body class="bg-slate-50">

    <!-- Navbar -->
    <nav class="glass-effect sticky top-0 z-50 px-4 py-3">
        <div class="max-w-5xl mx-auto flex justify-between items-center">
            <div class="flex items-center gap-2">
                <div class="w-10 h-10 ai-gradient-bg rounded-xl flex items-center justify-center text-white shadow-lg">
                    <i class="fas fa-robot text-xl"></i>
                </div>
                <span class="font-bold text-xl tracking-tight italic text-slate-800">Halit Evin <span class="text-blue-600">Aİ</span></span>
            </div>
            <div class="flex items-center gap-3">
                <span class="hidden sm:inline text-xs font-bold text-green-600 bg-green-50 px-3 py-1 rounded-full">AKTİF</span>
                <button onclick="location.reload()" class="text-slate-500 hover:text-blue-600 transition"><i class="fas fa-redo-alt"></i></button>
            </div>
        </div>
    </nav>

    <!-- Ana İçerik -->
    <main class="flex-1 max-w-5xl mx-auto w-full p-4 md:p-6 flex flex-col">
        
        <!-- Karşılama Ekranı -->
        <div id="welcome-view" class="flex-1 flex flex-col items-center justify-center text-center space-y-8 animate-fadeIn">
            <div class="space-y-4">
                <h2 class="text-4xl md:text-5xl font-extrabold text-slate-900 leading-tight italic">
                    Akıllı Ev <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-purple-600">Yardımcınız</span>
                </h2>
                <p class="text-slate-600 text-lg max-w-xl mx-auto">
                    Ev işleri, çocuk eğitimi ve günlük yaşam hakkında Halit Evin Aİ'ye her şeyi sorabilirsiniz.
                </p>
            </div>

            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 w-full max-w-4xl">
                <button onclick="startChat('Ev İşleri')" class="bg-white p-6 rounded-3xl shadow-sm border border-slate-100 hover:border-blue-300 hover:shadow-md transition group text-center">
                    <i class="fas fa-broom text-3xl text-blue-500 mb-3 group-hover:scale-110 transition"></i>
                    <h3 class="font-bold text-slate-800 block">Ev İşleri</h3>
                </button>
                <button onclick="startChat('Çocuk Gelişimi')" class="bg-white p-6 rounded-3xl shadow-sm border border-slate-100 hover:border-green-300 hover:shadow-md transition group text-center">
                    <i class="fas fa-child text-3xl text-green-500 mb-3 group-hover:scale-110 transition"></i>
                    <h3 class="font-bold text-slate-800 block">Çocuklar</h3>
                </button>
                <button onclick="startChat('Mutfak & Yemek')" class="bg-white p-6 rounded-3xl shadow-sm border border-slate-100 hover:border-orange-300 hover:shadow-md transition group text-center">
                    <i class="fas fa-utensils text-3xl text-orange-500 mb-3 group-hover:scale-110 transition"></i>
                    <h3 class="font-bold text-slate-800 block">Mutfak</h3>
                </button>
                <button onclick="startChat('Genel')" class="bg-white p-6 rounded-3xl shadow-sm border border-slate-100 hover:border-purple-300 hover:shadow-md transition group text-center">
                    <i class="fas fa-lightbulb text-3xl text-purple-500 mb-3 group-hover:scale-110 transition"></i>
                    <h3 class="font-bold text-slate-800 block">Soru Sor</h3>
                </button>
            </div>
            
            <p class="text-[10px] text-slate-400 uppercase tracking-widest font-bold">Yapay Zeka Destekli Aile Rehberi</p>
        </div>

        <!-- Sohbet Ekranı -->
        <div id="chat-view" class="hidden flex-1 flex flex-col max-w-3xl mx-auto w-full bg-white rounded-3xl shadow-2xl overflow-hidden border border-slate-200">
            <!-- Sohbet Başlığı -->
            <div class="bg-slate-50 px-6 py-4 border-b flex items-center justify-between">
                <div class="flex items-center gap-3">
                    <button onclick="goBack()" class="w-8 h-8 flex items-center justify-center rounded-full hover:bg-slate-200 transition text-slate-600">
                        <i class="fas fa-arrow-left"></i>
                    </button>
                    <div>
                        <h3 id="category-title" class="font-bold text-slate-800 leading-none">Halit Evin Aİ</h3>
                        <span class="text-[10px] text-blue-600 font-bold">YAZIYOR...</span>
                    </div>
                </div>
            </div>

            <!-- Mesaj Alanı -->
            <div id="chat-messages" class="chat-area overflow-y-auto p-6 flex flex-col bg-[#fcfcfc]">
                <!-- Mesajlar buraya gelecek -->
            </div>

            <!-- Yazıyor Göstergesi -->
            <div id="ai-loading" class="hidden px-8 py-2">
                <div class="dot-pulse">
                    <div class="dot"></div>
                    <div class="dot"></div>
                    <div class="dot"></div>
                </div>
            </div>

            <!-- Giriş Alanı -->
            <div class="p-4 bg-white border-t">
                <div class="flex items-center gap-2 bg-slate-100 rounded-2xl p-2 focus-within:ring-2 focus-within:ring-blue-500 transition-all">
                    <input type="text" id="user-prompt" placeholder="Bana bir şey sor..." class="flex-1 bg-transparent border-none outline-none px-3 py-2 text-slate-700">
                    <button onclick="handleSendMessage()" class="w-12 h-12 ai-gradient-bg text-white rounded-xl flex items-center justify-center hover:opacity-90 transition shadow-lg">
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
            </div>
        </div>
    </main>

    <script>
        const apiKey = ""; // Otomatik olarak doldurulur
        let activeCategory = "Genel";

        function startChat(cat) {
            activeCategory = cat;
            document.getElementById('welcome-view').classList.add('hidden');
            document.getElementById('chat-view').classList.remove('hidden');
            document.getElementById('category-title').innerText = "Halit Evin - " + cat;
            
            const container = document.getElementById('chat-messages');
            if(container.children.length === 0) {
                renderMessage('ai', `Merhaba! Ben Halit Evin Aİ. Bugün "${cat}" konusunda size nasıl destek olabilirim?`);
            }
        }

        function goBack() {
            document.getElementById('chat-view').classList.add('hidden');
            document.getElementById('welcome-view').classList.remove('hidden');
        }

        function renderMessage(role, text) {
            const container = document.getElementById('chat-messages');
            const msgDiv = document.createElement('div');
            msgDiv.className = `message-bubble ${role === 'user' ? 'user-message' : 'ai-message'}`;
            msgDiv.innerText = text;
            container.appendChild(msgDiv);
            container.scrollTop = container.scrollHeight;
        }

        async function handleSendMessage() {
            const input = document.getElementById('user-prompt');
            const message = input.value.trim();
            if (!message) return;

            renderMessage('user', message);
            input.value = '';

            const loading = document.getElementById('ai-loading');
            loading.classList.remove('hidden');

            const systemPrompt = `Sen Halit Evin Aİ'sin. Kibar, aile dostu, Türkçe konuşan bir yardımcı yapay zekasın. 
            Şu an "${activeCategory}" kategorisinde kullanıcıya yardımcı oluyorsun. 
            Pratik, kısa ve faydalı bilgiler ver. Tıbbi teşhis koyma.`;

            try {
                const response = await fetchWithRetry(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        contents: [{ parts: [{ text: message }] }],
                        systemInstruction: { parts: [{ text: systemPrompt }] }
                    })
                });

                const data = await response.json();
                const aiText = data.candidates?.[0]?.content?.parts?.[0]?.text || "Üzgünüm, şu an cevap veremiyorum.";
                renderMessage('ai', aiText);
            } catch (err) {
                renderMessage('ai', "Bağlantı hatası oluştu, lütfen tekrar deneyin.");
            } finally {
                loading.classList.add('hidden');
            }
        }

        async function fetchWithRetry(url, options, retries = 3) {
            for (let i = 0; i < retries; i++) {
                try {
                    const response = await fetch(url, options);
                    if (response.ok) return response;
                } catch (e) {
                    if (i === retries - 1) throw e;
                }
                await new Promise(r => setTimeout(r, 1000 * Math.pow(2, i)));
            }
        }

        document.getElementById('user-prompt').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') handleSendMessage();
        });
    </script>
</body>
</html>

